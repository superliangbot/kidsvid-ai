import { createLogger } from '@kidsvid/shared';

const logger = createLogger('feedback');
logger.info('Feedback CLI — not yet implemented. Use orchestrator for the full pipeline.');
logger.info('Available modules: PerformanceTracker, StrategyScorer, ReportGenerator');
