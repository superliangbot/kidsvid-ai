import { createLogger } from '@kidsvid/shared';

const logger = createLogger('publisher');
logger.info('Publisher CLI — not yet implemented. Use orchestrator for the full pipeline.');
logger.info('Available modules: YouTubeUploader, SeoOptimizer, UploadScheduler, PlaylistManager');
